# 사각형 면적 계산
# 함수 선언

def rect_area(a,b):
    # 면적 계산
    # 결과 출력
    r = a * b
    return r

# 함수호출
result = rect_area(3,4)   # 매개변수가 있는 함수임, input을 넣을 필요가 없슴 / 함수를 정의할때 프린트랑 인풋을 안쓰는게 좋음
print(result)
print(rect_area(5,10))